# Taskboard 優先度機能の実装 TODO

現在の `taskboard.js` には内部的に `priority` プロパティが存在しますが、UIから操作・表示できません。これを実用的な機能として拡張します。

## 1. 優先度の定義と表示 (Visuals)
- [ ] 優先度ごとのカラーパレット定義 (CSS変数)
    - P1 (High): レッド / 緊急
    - P2 (Medium): イエロー / 通常 (デフォルト)
    - P3 (Low): ブルー / 低
- [ ] タスクカード上に優先度バッジを表示する
    - `.cardTop` 内に表示
    - 優先度に応じた色分け (CSSクラス)

## 2. 優先度の編集機能 (Actions)
- [ ] タスク作成時に優先度を選択できるようにする
    - `prompt()` の代わりに、簡易的な選択UIの検討（または `prompt` で入力を受ける暫定対応）
- [ ] 既存タスクの優先度を変更するボタンを追加
    - `Rename` ボタンの横に `Priority` ボタンを追加
    - クリックで P1/P2/P3 をトグル、または選択

## 3. データ永続化と履歴 (Persistence)
- [ ] 優先度変更時に `tasks.ndjson` に保存する
- [ ] 優先度変更イベントを `events.ndjson` に記録する
    - `type: "priority_changed"`

## 4. 応用機能 (Advanced) - 任意
- [ ] 優先度によるフィルタリング機能
    - 画面上部に「P1のみ表示」などのフィルタボタンを配置
- [ ] 優先度順のソート
    - カラム内で優先度が高いものを上に表示するオプション
